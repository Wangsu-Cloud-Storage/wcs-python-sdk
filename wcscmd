import os
import sys
from optparse import OptionParse, Option
from logging import debug, info, warning, error

def output(message):
    sys.stdout.write(message + '\n')
    sys.stdout.flush()

def main():


if __name__ == '__main__':
    from wcs.commons import config
    main()

